<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.kanemochi.dao.AndroidMapper">
	
	<select id="getCalendarData" parameterType="RecordVO" resultType="RecordVO">
		select category,record_tag,record_price,record_pay from RECORD where user_id = #{user_id} and TO_CHAR(record_date,'YYYY-MM-DD') = #{record_date}
	</select>
	
	
</mapper>

