<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.kanemochi.dao.GPMapper">
	<!-- 스크린샷 저장 -->
	<insert id="screenshotSave" parameterType="ScreenshotVO">
		insert into screenshot values(seq_screenshot.nextval,#{user_id}, sysdate,#{screenshot_url})
	</insert>

</mapper>
